apiVersion: apps/v1
kind: Deployment
metadata:
  name: fusion-app-operator
  namespace: fusion-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: fusion-app
  template:
    metadata:
      labels:
        name: fusion-app
    spec:
      serviceAccountName: fusion-app
      containers:
        - name: fusion-app
          # Replace this with the built image name
          image: registry.cn-hangzhou.aliyuncs.com/njuicscn/fusion-app:201911141700
          command:
          - /usr/local/bin/fusionapp-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "fusion-app"
            - name: MQ_ADRESS
              value: "221.228.66.83:30595"
